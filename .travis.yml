language: c++

#dotnet cli require Ubuntu 14.04
sudo: required
dist: trusty

#dotnet cli require OSX 10.10
osx_image: xcode7.1

addons:
  apt:
    packages:
    - gettext
    - libcurl4-openssl-dev
    - libicu-dev
    - libssl-dev
    - libunwind8
    - zlib1g

os:
  - osx
  - linux

env:
  matrix:
    - CLI_VERSION="1.0.0.001494"
    #- CLI_VERSION="1.0.0.001598" #next_lkg
    - CLI_VERSION="Latest"

matrix:
  allow_failures:
    - env: CLI_VERSION="Latest"
    - os: linux

before_install:
  - find ./scripts -name "*.sh" -exec chmod +x {} \;
  - ./scripts/show-dotnet-info.sh
  #- if test "$TRAVIS_OS_NAME" == "osx"; then brew update; brew install icu4c; fi

  # Download script to install dotnet cli
  - curl -L --create-dirs https://raw.githubusercontent.com/dotnet/cli/rel/1.0.0/scripts/obtain/install.sh -o ./scripts/obtain/install.sh
  - find ./scripts -name "*.sh" -exec chmod +x {} \;
  - export DOTNET_INSTALL_DIR="$PWD/.dotnetcli"
  # use bash to workaround bug https://github.com/dotnet/cli/issues/1725
  - sudo bash ./scripts/obtain/install.sh --channel "beta" --version "$CLI_VERSION" --install-dir "$DOTNET_INSTALL_DIR"
  # set DOTNET_HOME env var and add dotnet bin to PATH
  - export PATH="$DOTNET_INSTALL_DIR:$PATH"
  # Workaround "missing default.win32manifest"
  - sudo curl -L "https://raw.githubusercontent.com/Microsoft/visualfsharp/coreclr/src/windows/default.win32manifest" -o "$DOTNET_INSTALL_DIR/sdk/1.0.0-beta-002060/default.win32manifest"

script:
  - ./scripts/show-dotnet-info.sh
  # Workaround "Too many open files"
  - ulimit -n 1024
  # Run dotnet new 
  - mkdir -p "test/test-dotnet-new" && pushd "test/test-dotnet-new"
  - dotnet new --lang f#
  - dotnet restore
  - dotnet --verbose build
  - dotnet --verbose run a b
  - popd
  # Run all tests 
  - ./scripts/run-tests.sh
